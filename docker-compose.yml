version: "3.9"

services:
  discord-bot:
    build: .
    container_name: discord-bot
    restart: unless-stopped
    working_dir: /app
    command: python main.py

    # Load secrets safely
    env_file:
      - .env

    # Optional: map source code for live reload (remove in production)
    volumes:
      - .:/app
